# Third-party sources

`ffmpeg` is a Git submodule pinned to tag `n8.0`, commit
`140fd653aed8cad774f991ba083e2d01e86420c7`.

No local FFmpeg patches are required. If patches are introduced later, place
them in `ffmpeg-patches`, document their order, and include them in the build
configuration hash and complete-source release.
